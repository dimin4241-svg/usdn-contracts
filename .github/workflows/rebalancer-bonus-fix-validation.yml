name: USDN Rebalancer bonus rounding fix validation

on:
  push:
    branches:
      - security/usdn-rounding-fix-rebalancer-bonus-validation

permissions:
  contents: read

jobs:
  proof:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: foundry-rs/foundry-toolchain@v1
        with:
          version: nightly-50dd4b8accae878391b0825885f0632d1e0a1e4c
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci && forge soldeer install
      - name: Active Rebalancer pending-assets bonus conservation
        run: forge test --match-contract TestLiquidationRoundingRebalancerBonusValidation -vvvv
      - name: Upstream Rebalancer invariant suite
        run: forge test --match-path 'test/unit/UsdnProtocol/Long/_TriggerRebalancer.t.sol' -vvvv
